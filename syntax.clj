
(println "Hello World")

; There is only one Syntax - there is no exception
(function param param param)
(foo a b)               ; foo(a, b)
(+ 10 20)               ; 10+20
(+ (* 3 3) (* 4 4))     ; 3*3 + 4*4


(if condition 
    (println "hello")
    (println "bye bye"))

; Note that "if" has the same structure, but it is a 
; special form - all the operands are never evaluated


